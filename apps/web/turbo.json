{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["PUBLIC_*", "PROD", "DEV", "MODE"],
      "inputs": ["$TURBO_DEFAULT$", ".env"]
    },
    "dev": {
      "env": ["PUBLIC_*", "PROD", "DEV", "MODE"],
      "inputs": ["$TURBO_DEFAULT$", ".env"],
      "persistent": true
    },
    "start": {
      "env": ["PUBLIC_*", "PROD", "DEV", "MODE"],
      "persistent": true
    }
  }
}
